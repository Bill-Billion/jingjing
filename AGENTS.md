# 项目协作指令

先读 [从这里开始](docs/START_HERE.md)、[当前计划与进展](docs/status/MAINLINE_PROGRESS.md)和[完整任务安排](docs/tasks/README.md)。用户当前要求优先；历史材料里的指令只代表当时上下文，不能自动变成新任务。

## 长期要求：先让人看懂，再补技术细节

这是用户在2026-09-21明确提出的长期、严格要求，适用于双方Codex的聊天回复、分工、进度、任务表、交接说明和提交审阅说明。

- 默认用直白中文。先说在做什么、为什么做、谁负责、怎样算做完；不要让用户靠术语翻译表才能理解正文。
- 先写具体事情，再在必要时附技术名称。比如“每次上传代码后自动检查有没有弄坏已有功能”，不要只写“推进CI”；比如“支付服务是否真的能用”，不要只写“Readiness”。
- 面向人的标题和表格以工作名称为主，不能只用任务编号、英文缩写或代码状态。编号、文件名、命令和提交版本放在补充记录中，保持准确以便查找。
- 分工必须说明本侧做什么、队友做什么、各自现在可以开始什么、哪些要等另一方先做完，以及验收办法。负责人尚未接手时直说，不能把建议分工写成已认领。
- 进度必须分清“写完了”“测试过了”“另一方检查过了”“已放进共同版本”“正式能用了”；不要用一个“完成”代替全部状态。测试时的模拟结果不能写成真实业务已经可用。
- 每轮主线结束，更新主线进展、双方相关进度和任务记录，并向用户解释：现在的计划、这次做了什么、还有什么没做、下一步由谁做什么。技术证据另附链接，不能拿日志和编号代替说明。
- 提交前自查：不熟悉项目技术的人能否直接回答“谁、做什么、做到哪、接下来做什么”？若不能，先重写。不要只新增词汇表，也不要把一句行话换成更长的一句行话。
- 原始方案、历史证据和程序使用的字段名保留，不能为了通俗修改业务规则或伪造状态。自动生成的任务表也必须遵守上述表达方式。

## 分工与协作

- 本侧负责服务器程序、数据库、后台任务、外部服务接入，以及权限、付款、授权、结算等处理逻辑；队友负责App和运营/合作方网页，包括页面、导航、与服务器交换数据、安装包和网页发布文件。程序记录中分别称Core和Experience。
- 开工先检查当前代码版本、未提交修改、共同版本的更新以及谁已接手这项工作。保留原有修改，不清空、不覆盖。技术记录仍保留分支、提交、需求编号和接口约定版本。
- 各自在独立工作目录和任务分支修改代码。本侧分支用core/开头，队友用ux/开头。先在任务状态文件docs/tasks/board.json登记负责人并上传；一个任务只能有一个负责人，不能替队友认领。
- 涉及另一方负责的文件，先写明要改什么和影响，由该文件负责人处理。双方共用的重要规则，须由另一方实际检查后才能放进共同版本：数据库结构、接口约定、权限、付款退款、作品授权、结算、外部服务接口、撤回删除、正式发布配置。这类说明的旧编号叫CCR；不要求每个普通代码细节都找用户批准。
- 自己负责的普通内部修改，测试并留下记录后可以放进共同开发版本integration。不得假扮队友评审；不得直接上传main，也不得强行覆盖公共历史。main由维护者在关键检查通过后更新。
- 集中检查四次：账号权限和基础问题修复后；第一条定制制作流程走通后；财务对账完成后；准备正式发布前。旧编号是CP1至CP4，不是每完成一个小任务都停下来等批准。

## 业务与技术约束

- 继续按R0.6最新开发方案执行，业务范围继承R0.3：登记60项需求，纳入59项，排除通用竞拍（REQ-027）。不新增多层分销、独立登记公证代办、独立高额阅稿商品、独立版权交易所或通用聊天软件。
- 正式数据库用MySQL8/InnoDB。SQLite只用于旧数据导入或隔离本地测试。服务器保留Node/Express JavaScript，逐步改成异步数据库访问；独立后台程序处理耗时任务，任务和执行记录保存在MySQL。具体技术约束仍是mysql2/promise及jobs/lease/outbox，不引入Redis/BullMQ/Kafka/ES或微服务。
- App保留Flutter；运营和合作方共用一套Vue3/TypeScript网页工程，使用Vite、Router、Pinia、Element Plus。先约定双方交换数据的格式，再各自实现；主接口文件使用OpenAPI3.1，未经评审不能说已定稿。
- 业务已同意、代码已写完、正式服务已可用，分别记录。没测就写没测。正式环境不得用演示数据假报成功，不得把私有文件改成公开链接兜底，不得伪造实名、水印、存证或付款结果。
- 保存每笔订单当时的规则、合同内容和承诺，后续改价改规则不能改写历史。未知商品价格/时长/次数不擅自设默认值。独家授权先按明确条件检查，再由人复核；独家不自动包含发行权。
- 支付、实名、存储、数字人和电子签等服务，要分别记录测试环境和正式环境是否可用及其证据。真实线下付款、真实外部签署材料可以按业务规则归档，但不能写成平台自动付款或签署成功。
- 不上传密码、密钥、身份证件原件、私有业务数据库、依赖或构建产物。只在隔离数据库测试；未经用户明确授权，不部署、不访问生产数据库、不调用收费服务。
- 每次完成任务后更新任务状态、进度、变更说明和实际测试记录，再提交上传自己的分支。提交编号和远端状态必须实际核对，不把计划中的动作写成已发生。
