# 合同和历史规则读取：已接通并完成本地联合验证

2026-09-24继续核对：第11份申请仍开放，无新审阅意见；共同版本仍7d139ff。为了接可信登录，本侧已将其d2e6c93代码合入本地第三阶段分支，合并提交cb4d7cf。三个进度/任务文件冲突已保留第三阶段进度并补齐第二阶段实际状态；没有修改远端申请、替队友批准或合入共同版本。

## 本轮成果

- 接通两个GET：读取获准查看的未签署合同JSON内容，以及该合同当时采用的规则正文。不是任意规则库查询，不产生签约、付款或许可生效结果。
- 每次检查有效会话、当前身份成员关系、合同当事方范围和这份合同的独立读取名单。退出、会话过期、账号停用、成员关系撤销或读取授权撤销后均拒绝。
- 合同保存后规则后来停用仍能按旧合同读取；别的规则版本不能借同一入口读取。存储内容校验失败返回明确服务错误，不泄露正文或数据库错误。
- 新内容接口不伪造私有文件编号；旧文件元数据接口继续未实现。公开创建、审批或封存写入口未开放，正式运营权限和真实业务来源仍默认拒绝。
- 迁移0020–0024原样从计划目录移入正常升级目录；未改旧SQL。已验证认证表0015–0019之后升级、重复升级和新建空库。未访问生产。

调用方式和分工见[读取接口说明](../../collaboration/STAGE_3_READ_API.md)。候选格式升至0.3.0-rc.1，共24个操作，19个有隔离实现，5个仍未实现；新增两个接口，不改此前账号格式。前端按文本渲染条款，不执行其中HTML。

## 实际验证与修复

合并后的本地完整回归184项通过，1项缺脱敏旧SQLite样本跳过。规则合同专项36项通过，账号HTTP专项19项通过；数据库、后台任务、外部服务、账号机构和旧问题专项也通过。这是联合分支的结果，各组重叠，不相加为业务完成量。

实际HTTP返回校验：账号19份返回对应723项检查通过，合同/规则3份返回对应707项检查通过；公共示例28项、反例27项。样本全为合成数据，不包含可用登录令牌。新的远端工作流已纳入合同实际返回校验，但本轮未推送，不能写成GitHub已运行。

测试发现条件请求中的If-None-Match通配符会触发Express隐式304；新敏感内容接口改为每次校验后明确返回正文，不缓存为成功。另修正测试中的成员撤销枚举，使其使用实际数据库值REVOKED。接口文档生成首次因Windows默认文本编码读取失败，指定UTF-8后补齐并校验；这些失败均未计为通过。

[完整回归](CORE-S1-003-http-evidence/local-summary.json) · [合同专项](CORE-S1-003-http-evidence/local-governance.log) · [合同实际返回格式](CORE-S1-003-http-evidence/governance-contract.json) · [源码与边界记录](CORE-S1-003-http-evidence/validation.json)。

继续执行时测试数据库已自行停止，停止命令返回连接拒绝；随后核实没有MySQL进程、33316监听，核对并撤销本工作目录的J盘映射。没有把这次连接拒绝算作应用测试失败或声称由本轮停止了运行中的进程。

## 当前计划和剩余工作

第三阶段的内容封存、内部数据库保存、读取授权与只读HTTP已在本地验证。正式规则审批人员权限、经过验证的真实合同/订单写入来源、业务启用条件检查仍待实现/接线；第三阶段与整个账号/合同任务不能标为全部完成。

本侧继续这些写入与启用边界，并准备统一阶段交付；第11份进入共同版本后再核对依赖及合并结果。队友可继续审阅第11份和准备账号页面，合同内容展示按候选说明准备；本轮材料未推送，不能当作队友已经收到。未代为认领前端任务。

本轮只保存本地提交，不推送、不新建申请、不部署。正式启用、真实短信、外部签署和前端联调均未完成；原业务范围、未知商品参数和资金/授权规则不变。
