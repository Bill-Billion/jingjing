# 规则操作权限：已接数据库并提供受控维护工具

本轮开工312980b、工作目录干净；共同版本仍7d139ff。第11份账号接口申请仍开放，无新评论或审阅；本地继续使用此前明确引入的待审依赖，没有合入远端共同版本。

## 已做工作

新增逐操作权限记录与逐版本变更历史（迁移0025–0026），不修改旧迁移。编写、读取规则库、提交审核、批准、生效、停用和发起封存分别授权，不因持有其中一项而自动取得其他项，也不因机构成员身份自动获得平台权限。合同正文读取仍另查每份合同的读取名单。

规则仓库默认查MySQL权限，不再依赖测试中的“允许所有操作”回调。账号有效、权限启用且未到期才允许操作，重复请求也重新核对。权限维护先锁定账号，再更改权限，原版本不符就拒绝；权限和历史一起保存，失败全部回滚。

新增本机维护工具，默认只检查文件并预览；明确--apply才连接显式配置的MySQL执行。没有公开自助授权接口或自动管理员初始化。维护数据库凭据是权限边界，填写authority_ref仅记录依据，不代表自动验证了业务批准。操作人员实际配置尚未进行，本轮只创建隔离测试账号的合成授权。

使用办法和限制见[审批权限说明](../../collaboration/GOVERNANCE_OPERATOR_ACCESS.md)。前端接口格式未改变，队友这轮无需修改页面。

## 验证

当前分支完整回归190通过、1项因缺脱敏旧SQLite样本跳过；规则合同专项42通过、账号HTTP19通过，其他既有专项全部通过。新增检查覆盖：无授权默认拒绝、只允许已授予操作、新建仓库实例仍读取同一授权、撤权后拒绝历史请求重放、到期和停用限制、并发版本冲突、历史记录失败回滚、维护命令默认不写库及显式执行才写入。

[本机回归汇总](CORE-S1-003-operator-evidence/local-summary.json) · [专项日志](CORE-S1-003-operator-evidence/local-governance.log) · [源码校验值与边界](CORE-S1-003-operator-evidence/validation.json)。测试结束随机库已清理，本机MySQL停止、33316关闭，临时J盘映射核对后撤销。

本轮没有重新运行接口格式校验，因为未修改HTTP/接口文档或返回结构；实际HTTP测试已随完整回归运行。不声称获得新的GitHub验证结果，没有访问生产、收费服务或给真实账号授权。

## 计划与下一步

第三阶段内容封存、MySQL保存、只读接口和操作权限执行机制已有本地成果；还需可信合同/订单写入来源、业务启用条件检查及阶段交付整理。真实人员配置、真实供应商及前端联调单独记录，不能把工具可用当成业务已启用。

本侧下一步先处理可信写入来源与启用检查。队友继续审阅第11份及账号页面；共享规则仍需实际审阅。当前代码仅本地提交，不推送、不新建申请、不部署，第三阶段仍进行中。
