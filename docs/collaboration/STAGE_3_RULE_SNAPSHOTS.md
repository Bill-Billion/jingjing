# 第三阶段：保存规则和合同，避免后来修改影响旧承诺

2026-09-24更新：本地已合入第11份依赖，两个只读HTTP、当前身份与读取授权以及0019→0024升级已验证；全套184通过/1跳过。正式运营权限、真实写入来源和业务启用检查仍待推进，不推送或新建申请。[本轮读取说明](STAGE_3_READ_API.md)。下方开工和早期状态为历史记录。

2026-09-23开始。本侧负责内容封存、数据库保存、权限和接口；队友后续负责显示具体版本、条款和真实签署状态，未替队友认领。第11份账号接口申请仍开放、无审阅意见；共同版本7d139ff。本分支core/stage-3-rule-snapshots直接从共同版本开始，不包含第11份代码。

## 开工范围与完成标准

1. 先独立实现内容校验、稳定的内容校验值、深拷贝封存和历史内容核对。不读数据库、不调供应商，不依赖待审登录接口。
2. 随后接入MySQL不可覆盖的版本保存、权限和操作记录。保留第一阶段数据库能力；认证表编号0015–0019已在第11份申请使用，接入时统一检查迁移顺序，不能在不同分支重复占号。
3. 账号接口进入共同版本或依赖安排明确后，再接只读接口与实际HTTP测试。阶段验收须证明改新版本不改旧记录、越权不可读、重复提交与并发可靠。

第1项已完成。本轮第2项内部数据库实现已通过隔离测试：保存、独立读取授权、版本检查、重复请求和操作记录已实现；正式运营权限及真实合同/订单来源尚未接线。整个第三阶段仍在进行，不因工具函数测试通过就集中推送或新开申请。公开HTTP尚未实现，不能给页面直接调用。业务启用条件另行接入，不能由“封存成功”替代。

## 必须保留的边界

- 不补未知价格、时长、修改次数、退款或授权条件；内容由后续经过校验的业务流程提供。
- 只封存未签署内容；不凭一个签署方式字段或任意证据编号认定已签署。真实签署、付款和许可生效另有验证。
- 草稿、待审核、未来生效或停用规则不能用于新的封存。封存后的历史内容不因规则后来停用而消失。
- 校验值只能帮助发现内容变化，不是可信签名、公证或防数据库管理员篡改系统。读取权限和来源必须由数据库/服务层保证。
- 此模块是内部内容格式，不能把它当成已经批准的新公开接口。

本阶段只本地开发和记录；完整阶段自验与交接齐备后才集中上传，重要共享规则仍由另一方审阅后合入。

2026-09-23数据库增量：计划迁移0020–0024先放在planned-governance，由隔离测试临时拼接验证，不改变正常升级流程。当前分支完整回归160通过/1缺脱敏旧库跳过，规则合同专项31通过。第11份仍待审，本阶段未推送，完整升级次序及HTTP接线尚未验证。
