# 请另一方检查：外部服务状态和私密文件保存

本侧已实现这项基础改动，另一方尚未检查，不加入共同版本。编号CORE-S0-005；负责人chengcongcong222。涉及数据库表、外部服务调用、文件权限和删除，所以依照项目共同规则记录供另一方检查。

请重点看这些实际影响：

1. 新增两张表保存服务状态和操作历史；验证必须区分用途、环境与当前配置。改状态要检查权限，标成验证通过还要核查证据，默认都拒绝。新增0006、0007，不改旧迁移文件。
2. 新私密文件接口默认无权限，上传明确私有且不覆盖，读取/链接/删除每次检查权限。临时链接到期前仍有效，不能承诺即时撤回已签发链接。正式云端权限尚未验证。
3. 旧存储不再回退公开目录，旧AI/数字人上传可能明确失败，必须迁移对应业务后恢复；不能用公开假链接维持旧演示。
4. `/health`代表能响应，`/ready`代表必要依赖可用。旧SQLite应用不报告已准备接正式流量。运营查看/修改服务状态的新入口尚未开放，等待真正的账号和机构权限。
5. 日志只保留允许的关联字段，不记录敏感正文；供应商超时可能已受理，不自动重试。真实支付、实名、电子签等新接入还没完成。
6. 这轮不修完所有旧业务漏洞。下一项继续处理假付款、实名自动批准、付款查询归属、假水印/存证等。不能把基础检查当作这些旧路由已经安全。

本次分支基于自动检查分支，仍依赖前面的MySQL和后台任务改动；按前置改动顺序检查并合入，再将本次合并目标改为integration核对最终差异。不能通过把本次合进前置任务分支来代替另一方检查。接口参考第1份合并申请的草案，尚未定稿。

[本次成果和测试](../tasks/records/CORE-S0-005.md) · [给开发者的使用说明](../../晶晶日上工程交接包/01_源码/backend_server/docs/PROVIDER_FOUNDATION.md)。检查状态PENDING，未代签。
