# 请另一方检查：旧付款、实名、存证和水印不再假报成功

本侧已完成第一批修复并通过本机和GitHub测试，尚未经另一方检查，不进入共同版本。负责人chengcongcong222，查找编号CORE-S1-001；整组旧问题仍在进行。第一批未改数据库；2026-09-22第二批新增旧SQLite补列，未改正式MySQL迁移。两批均未修改App/网页。

请具体检查：

1. 旧样片付款按钮只返回未启用，不再改为已付；旧支付、票据、回调和默认退款服务也不能绕过。原先能走的演示付款暂时不能继续，真实流程待后续支付任务完成。
2. 个人实名未启用时不受理、不保存新证件。历史approved不被当作已核验，因为旧表没记来源；原记录保留，响应明确待复核。企业材料仍只能进入待审。
3. 依赖实名的旧绑收款账户、提现、人脸核身入口暂不放行；不是取消产品能力，不能靠旧布尔标志继续操作。
4. 两套旧付款查询都限制归属；admin这个泛化角色不能绕过个人查询。历史付款响应区分原来记录与当前是否核实，金额单位不在本轮改动。查询不再隐式调用供应商改订单。
5. App需要正确展示503、未受理和历史待核验，不能由原来的Demo回退显示成功。字段和兼容说明在下方代码文档；正式新接口仍按已约定的格式推进。
6. 原退款核算测试继续保留，成功仅由测试文件显式注入替身。默认业务服务没有Mock通道，未声称真实退款成功。历史数据没有批量改写，尚缺脱敏样本完成对账。

本轮依赖第6份申请及此前基础代码；先按依赖顺序检查合入，再把本轮申请目标改为integration核对差异。不得把合到前置任务分支当作另一方已检查。权限、支付与外部服务变更按项目约定等待实际检查，未代签。

[本轮成果](../tasks/records/CORE-S1-001.md) · [接口行为与兼容说明](../../晶晶日上工程交接包/01_源码/backend_server/docs/LEGACY_SAFETY.md) · [所有旧问题逐项进度](../status/LEGACY_ISSUES.md)。

代码见[第7份合并申请](https://github.com/Bill-Billion/jingjing/pull/7)，尚未合并。

## 第二批需要检查的具体影响（2026-09-22）

1. 新增旧SQLite迁移006：sample_library.video_url可空、scripts.digest_kind可空。旧001–005不变，已有列和数据不覆盖；不表示正式数据库退回SQLite。
2. 敏感字段密钥不再默认或截断；生产启动必填32个UTF-8字节，开发填错也拒绝。现有合法AES-GCM密文格式继续可读；错误密钥和损坏密文不能当空身份继续。旧长密钥需核对实际使用字节后制定换钥，不自动猜测或重写数据。
3. 投稿只报保存及提交信息摘要，evidenceHash为null；新字段submissionDigest、digestScope、evidenceStatus和trustedTimestamp明确证据边界。历史摘要保留但不认证。本人投稿仍可查看，他人剧本浏览暂503，等待完整权限和保密约定流程。
4. 三类旧交付入口在登录检查后返回503、delivered=false，不改订单，不加工文件。水印占位函数不生成假编号，即使填写服务名仍不可用；重新启用须真正处理并验证文件。
5. 生成任务审核通过不是交付证明；历史订单状态不批量改写。页面不得把以上拒绝、空证据或历史标记改成演示成功。

[第二批成果与测试](../tasks/records/CORE-S1-001-20260922.md)。整组仍在做，尚未替另一方批准数据库结构、权限和交付行为变化。
