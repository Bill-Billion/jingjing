# 请共同检查：账号登录与机构邀请接口

本次由本侧实现，队友检查后才能合入；没有代签审阅。任务CORE-S1-002，第二阶段分支core/stage-2-account-api，基于共同版本7d139ff。

具体交付与启动方法见[第二阶段说明](STAGE_2_ACCOUNT_API.md)。新增17个可隔离验证的操作（含原有探针），新增5个数据库表。接口候选从0.1升至0.2；5个其他领域操作继续未实现。

请检查：

1. 页面能否按短信申请→登录→读取身份→携带所选身份调用完成操作；旧JWT和旧用户编号不能混用。
2. 邀请人只提供对方的新账号ID与明确截止时间，受邀本人接受/拒绝；发送邀请不授予商业或收款权限。暂无手机号搜人。
3. 机构/能力申请显示待审；缺短信验证显示未启用。HTTP错误不能被页面替换为演示成功。
4. 分页、If-Match、Idempotency-Key和原结果重放规则是否满足页面；同键重试不能产生第二个会话或邀请。
5. 技术参数可配置，真实短信验证、旧账号迁移、密钥轮换和前端联调边界是否清楚。安装包、运营审核等后续任务不被本轮声明为完成。

恢复：不编辑既有0001–0014；新增0015–0019应用后代码回退不删除表或认证记录。没有生产数据操作；正式升级/密钥轮换必须有备份与迁移方案，不能靠切回旧JWT或公开存储兜底。

另一方审阅结果：未提供。用户/业务决策：沿用已明确的个人与机构分离、多能力待审、本人确认，无新增商业参数。真实服务证据：未验证，不启用。
