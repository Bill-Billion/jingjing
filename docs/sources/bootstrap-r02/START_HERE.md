# 晶晶日上 / 晶选片场｜Codex首次接手资料包

**包版本：BOOTSTRAP-R02-20260920**  
**用途：首次对话；吸收三版需求与最新剧本版权增量，形成统一设计及本轮完整开发清单。**  
**范围口径：本轮统一规划和交付，内部按依赖顺序施工；不再用旧P0/P1/P2排除已纳入内容。**

## 1. 现在的实际状态

用户有两名开发者配合Codex，尚未向本地Codex发送第一条指令。曾经生成过旧启动包，但未执行；不能将旧包中“三天、先做私人定制、其余后置”的策略当成本轮指令。

工作目录：`D:\ccc\博士资料\7.项目\2026-晶晶日上`。私有仓库：`Bill-Billion/jingjing`。用户提供的协作者账号：`congcong222`。本地GitHub CLI登录与当前聊天的GitHub连接不是同一个认证环境。

本次整理读取了用户上传的仓库快照、V2文档、V3原件和R0.1/R0.2记录。没有取得当前远端commit，没有运行应用或真实支付/AI服务，没有修改代码。

## 2. 推荐目录，不要求工作区保持空白

```text
2026-晶晶日上/
├─ JX_CODEX_START_R02_20260920/  # 本资料包，先读不改
├─ repo/                        # 如果尚无仓库，由Codex克隆到这里
└─ intake_reports/              # 首轮核对报告
```

资料包先解压进工作区即可。不要在已有资料的目录执行`gh repo clone ... .`。若当前目录或其他子目录已有正确仓库，先核实再复用，不覆盖、不重复克隆、不清空文件。

## 3. 第一条消息

将`01_FIRST_MESSAGE.txt`完整复制到现有Codex对话。它包含目录检查、材料阅读、仓库访问、权限边界和首轮产出要求。

Codex首先显式阅读本包`AGENTS.md`。此文件放在资料包内时，不会自动约束兄弟目录`repo`；不能因文件名正确就假定仓库已加载。首轮只生成合并建议，保留仓库原有指令。

## 4. 分层阅读顺序

1. `AGENTS.md`：短规则，不塞全部历史。
2. `02_PROJECT_CONTEXT.md`：版本演进、明确方向、不能再误解的事项。
3. `03_CURRENT_SCOPE.md`：本轮目标、五类内容对象、完整工作范围及条件。
4. `04_REQUIREMENT_GOVERNANCE.md`：需求、参数、变更和验收如何记账。
5. `06_FIRST_PASS_TASKS.md`：第一次对话具体做什么、交什么。
6. `data/REQUIREMENTS_INDEX.md`与`data/requirements_intake.json`：逐项读取60项；不要只看摘要。
7. `sources/DELTA/`、`sources/V3/`与`extracted/`：回读原文，检查措辞、示例与待确认项。
8. `sources/V2/`、`sources/V1/`与`07_CODE_EVIDENCE_INDEX.md`：保留旧治理能力，核对旧工程事实。
9. `05_ENGINEERING_RULES.md`：详细设计及后续施工规范。

材料较多时分批阅读并记录已读章节；不要凭目录或搜索命中宣称“已完整读完”。

## 5. 原件、研究与交接层的关系

- `sources/V1/V2/V3`是相应版本的原材料副本；`extracted/`用于降低阅读成本。
- `sources/DELTA`是这次用户提供的新增文字，包含完整范围要求和样片六项的缺失说明。
- `sources/R01/R02`是之前研究暂定记录，不是逐项已签决议。
- `data/requirements_intake.json`是R0.2的无损索引，不是生产配置。
- 本包根目录指导文件是**交接规则及工程建议**，不冒充业务方原话。
- 原材料中的法规判断、供应商价格和产品能力，只作为待核验的历史研究；正式启用前重新核验，不继承“已合规”结论。

## 6. 首轮完成的定义

不是写几个页面，而是得到：当前仓库版本、资料覆盖情况、60项处置、冲突与缺件、目标设计、完整工作包、外部条件、测试及协作方案。

首轮允许只读代码、检查工具、在隔离环境中做获准测试及生成文档。**不改业务代码，不直接迁移现有数据库，不调用收费上游，不提交或推送，不部署。** 报告完成后停在设计审查点；这是开工前的一次确认，不是重新把产品拆成一期二期。

## 7. 快速检查资料包

安装了Python时，在资料包目录运行：

```powershell
py -3 .\tools\verify_package.py
```

该脚本仅检查本包文件指纹、需求编号和原决议空白状态；不联网、不启动工程、不读密钥、不修改项目。
