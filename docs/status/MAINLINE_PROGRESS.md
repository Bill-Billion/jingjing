# 主线进展与接续计划

更新：2026-09-21；执行基线 JX-R06-EXECUTION-BASELINE-20260921；当前 Stage 0。本页每轮主线结束更新，逐次证据保留在任务记录，不覆盖原方案/旧报告。

## 当前总体计划

|阶段|主要交付|进入后续工作的依据|
|---|---|---|
|S0 当前|契约、MySQL/迁移、持久任务、Provider/Readiness、CI、旧库导入准备；客户端工程基础|有可复验工程基础；共享边界交叉评审后集成|
|S1|14项旧问题按归属整改；账号/主体/权限；规则、合同和历史承诺快照|CP1基础/真实性检查|
|S2|数字人授权、作品版本、外部权利证据、许可商品/授予|规则、权限及供给事实可靠|
|S3|选本→许可交易→定制/项目制作→交付验收|CP2首条真实闭环|
|S4|商单、直接MCN协作、角色报名/邀请/报价、项目及发行台账|不恢复竞拍/多层分销/独立版权交易所|
|S5|三类资金事实、应付出款/发行收入/对账调整，业务通知与对象评论|CP3财务检查|
|S6|旧数据迁移、备份恢复、安全/负载、真实Provider、三端发布候选|CP4生产候选；候选不等于部署/上架|

60 项需求登记，59 项纳入，REQ-027 通用竞拍排除。核心业务范围继续完整推进，普通实现细节由各流负责；未知规格、合同/价格和外部权限不擅自设为正式规则。既有工时估算不是已承诺日历工期，需要结合评审周转、实测任务耗时和外部条件持续校准。

## 目前完成到哪里

|任务|代码/文档事实|集成与正式启用|
|---|---|---|
|CORE-S0-000 协作初始化|R0.6和来源入Git、30任务/60需求映射、Core认领与校验工具完成|DONE；已在 integration|
|CORE-S0-001 首批契约|OpenAPI候选0.1.0-rc.1、校验和证据已交付|PR #1 / IN_REVIEW；未冻结/未合并，本工作树不包含该候选主文件|
|CORE-S0-002 MySQL基础|异步连接/事务/迁移runner、拒绝SQLite生产回退，真实隔离库已自验|PR #2 / IN_REVIEW；旧路由和旧数据未整体迁移|
|CORE-S0-004 本轮Worker|持久任务、租约/心跳/token、outbox、重试审计、独立进程恢复，模块职责明确|IN_REVIEW；依赖PR #2，领域处理器未启用，不代表业务闭环完成|
|其他任务|尚未声称完成；Experience任务仍待实际负责人认领|不替队友签署完成或交叉评审|

本轮自验：Worker 21通过/0跳过；MySQL 16通过/0跳过；限定后端回归57通过/1跳过。唯一跳过是缺显式脱敏旧库快照。三组有重叠，不能相加。实测并发领取、过期接管、旧Worker被拒、事务回滚和实际子进程退出后恢复；未接入真实收费/生产服务。[本轮完整任务记录](../tasks/records/CORE-S0-004.md)；[机器证据](../tasks/records/CORE-S0-004-validation.json)。

旧API启动scheduler/视觉恢复已停用，原定时业务尚未移植，不能直接上线。空handler registry启动明确报WORKER_NOT_READY。整个Stage0未结束，尚无CP通过，现有App问题也没有因为队列测试通过而自动解决。

## 下一步顺序、分工与依赖

1. **Core下一主线：CORE-S0-006 后端CI和复验入口。** 把已有迁移/队列/限定回归接入干净环境中的隔离MySQL CI，缺配置/失败不能假绿；给队友一套统一复验命令。依赖MySQL基础，可在明确依赖分支推进，不等待所有业务决定。完成标准为CI实际运行证据和可复现说明，不能只提交YAML。
2. **CORE-S0-005 Provider/OSS/Readiness/日志/health。** 对齐PR #1候选，优先使真实配置缺失、私有存储失败和供应商未开通有明确状态；不把适配器壳子当接入成功。接口与权限边界仍需交叉评审。
3. **CORE-S0-003 导入与事实分类。** 可先编写映射、dry-run和合成异常测试；真实旧库数量/金额/关联对账需要显式提供的脱敏快照。假已付/未核验实名不得升级成真实事实，缺样本只阻断相应实证。
4. **进入S1先处理已确认工程风险。** 旧付款直接已付、实名缺配置自动批准、私有存储公开回退、支付查询归属、Demo/假水印存证、MCN无感添加、release调试签名等按Core/Experience归属落实对应断言；同时建设主体权限及不可变规则/合同/订单快照。
5. **Experience可独立推进UX-S0-001/003。** 现有Flutter工具链/五入口映射，以及单一A/B Web工程；继而设计系统和契约客户端。必须由队友实际认领，本侧不改前端或替他报完成。

评审工作并行推进：PR #1契约、PR #2 MySQL、Worker CCR-004。共享边界按AGENTS交叉review后才进integration；MySQL先合并，再调整Worker PR基线并检查差异。合并board/CURRENT保留两流新状态，不能整份覆盖。评审等待不阻止无冲突基础任务实现。

外部条件：队友实际任务认领/评审；脱敏旧库；支付/实名/电子签/数字人/OSS等对应权限和真实验证；合同/商品规格正式依据；iOS真机/签名构建环境等。未满足条件均保留未启用/待验状态，不用模拟成功补齐。

## 每轮交接固定内容

当前计划与所在阶段 → 本轮交付及累计完成/待评审状态 → 实际测试与限制 → 提交/PR和合入情况 → 下一项任务、依赖及可并行工作。同步更新board、CURRENT、逐任务证据和本机WORKSPACE_README；用户说明与仓库记录一致。

本轮本机收尾：便携MySQL已正常关闭，33316无监听，校验同一目录身份后撤销J盘临时别名；未注册系统服务。二进制/数据/随机口令保留在忽略的.local，可用于后续复验。

发布记录：实现提交 `f798f7925a63b0f43f50cb25f1a07c9698503917` 已推送，[PR #3](https://github.com/Bill-Billion/jingjing/pull/3) OPEN，base=`core/s0-002-mysql-foundation`。本段发布元数据在后续文档提交中记录；测试绑定文件hash不受影响。
