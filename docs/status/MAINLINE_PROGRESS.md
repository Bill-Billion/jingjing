# 主线进展与接续计划

2026-09-21；R0.6；Stage 0。后续每轮主线任务结束更新本页，同时保留逐任务证据。

当前推进 CORE-S0-004：模块边界、MySQL 持久任务/租约/outbox、独立 Worker。基于 PR #2 分支 d950950，integration 仍为 ac34c48；起点无未提交业务改动。REQ 按 board 中该任务的 24 项映射，契约 0.1.0-rc.1 仍在独立 PR #1 待评审，本分支不包含契约文件。

已完成协作基线；契约和 MySQL 基础已各自自验并提交 PR #1/#2，均未合并。Worker 本轮先认领，测试 NOT_RUN，不能视为已完成。

执行顺序：持久队列与 fencing → 事务 outbox → 独立 Worker 与恢复边界 → 隔离 MySQL 并发/重启/失败测试 → 证据、评审 PR 和下一轮计划。后续推进 Provider/Readiness 和 CI；旧库导入工具可独立准备，真实旧数据验收仍缺显式脱敏快照。Experience 可认领 Flutter 现状与 A/B Web 基础任务，本侧不代认领。

总体顺序：S0 工程基础 → S1 真实性整改/账号主体/规则快照（CP1）→ S2 权利与供给 → S3 许可交易与制作闭环（CP2）→ S4 商单/项目/发行 → S5 财务对账/通知评论（CP3）→ S6 迁移恢复及生产候选（CP4）。前期估算不是正式交付日期，依赖真实验收逐步校准。
