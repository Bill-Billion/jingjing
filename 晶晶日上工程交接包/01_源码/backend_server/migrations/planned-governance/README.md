# 待接入的规则与合同数据表

这些文件只供本阶段隔离测试使用，尚未放入mysql-runtime，普通升级命令不会执行。0020–0024预留在第11份申请的0015–0019之后，不能直接应用到缺少中间历史的正式数据库。

正式纳入共同版本前，先核对第11份申请与实际历史，统一迁移顺序，再验证“已有数据库升级”和“新建数据库”两条路径。本轮只测试了共同版本基础加这些计划表，不代表包含待审认证表的最终升级已验证。

无需更改既有迁移；不要为补编号修改已应用文件或手工删除历史记录。不提供生产回滚脚本。测试数据位于随机jx_test库，测试后清理；真实备份恢复与数据保留要求在后续发布阶段验证。
