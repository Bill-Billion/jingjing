# 晶晶日上 · 工程代码评审报告

> 评审日期：2026-09-09　｜　评审范围：`晶晶日上工程交接包`（847 文件 / 9.7MB）
> 方法：静态代码审查 + 全量检索取证，所有结论均附**文件路径 + 行号 + 代码片段**
> 立场：独立验证，不采信交接文档的自我描述。文档声称与实测不符处已单独标注。

---

## 0. 结论先行

**这是一个"文档治理水平远高于代码工程水平"的项目。**

它在**业务口径治理、合规设计、支付回调安全**三件事上做得比多数同规模创业项目更扎实；
但在**凭据管理、后端测试、类型安全、依赖卫生**四件事上停留在原型工程水准。

| 维度 | 评分 | 判断 |
|---|---:|---|
| 文档与知识传承 | 9 / 10 | 31 份档案 + 85 轮迭代日志，罕见的完整 |
| 业务口径一致性 | 8.5 / 10 | 金额 SSOT 执行到位，全库整数分 |
| 支付与资金安全 | 7.5 / 10 | 回调链路规范，但旧链路遗留 IDOR |
| 后端工程 | 6 / 10 | 结构清晰，测试近乎空白 |
| 前端架构 | 5.5 / 10 | 无模型层，弱类型传参，性能隐患明确 |
| **凭据与安全管理** | **2 / 10** | **明文 root 密码在包内，且脱敏声明失效** |
| 工程卫生 | 4 / 10 | 200+ 临时产物未清理，依赖分类混乱 |
| **综合** | **6 / 10** | 可作为"演示系统 + 业务蓝图"承接，**不能直接作为生产系统承接** |

**三条最该先做的事**：
1. **立刻轮换服务器 root 密码**（IP + 密码已在包内 4 个文件中明文暴露，见 §4.1）
2. **重新评估"未实名也能通过认证"的灰度分支**是否需要保留（见 §4.2）
3. **补后端测试**：当前 438 行，且未覆盖鉴权/支付/订单/实名四条命脉（见 §7）

---

## 1. 项目实测画像（文档声称 vs 实测）

| 项 | 文档声称 | 实测 | 判定 |
|---|---|---|---|
| 前端页面 | 40 页 | `lib/pages` 下 40 个 .dart | ✅ 一致 |
| 前端代码 | 77 文件 / 22,062 行 | 22,062 行 | ✅ 精确一致 |
| 后端路由模块 | 25 | 25 | ✅ 一致 |
| 数据库表 | 61 张 | MySQL DDL 61 张 | ✅ 一致 |
| 命名路由 | 27 条 | `app.dart` 实际 28 条 | ✅ 基本一致 |
| 后端测试 | 未明确量化 | **5 文件 / 438 行** | ⚠️ 远低于前端（5,976 行） |
| 主 Tab 保活 | "R1 未修：用 AnimatedSwitcher" | **已改为 IndexedStack + 懒挂载** | ❌ **文档滞后，代码已修** |
| 敏感项扫描 | "全部通过，未发现明文密码" | **4 个文件含明文 root 密码** | ❌ **严重失实** |

> **重要澄清**：`lib/widgets/main_scaffold.dart:38-99` 已实现 `IndexedStack` + `_visited` 懒挂载 + `TickerMode` 停后台 ticker，注释明确标注 "Gate0 F-R1"。
> 但交接总览 §9.1 仍写着"R1：用 AnimatedSwitcher 而非 IndexedStack，候选最高优先修复"。
> **承接方切勿照文档去重改一次已经改好的东西。** 这类"文档滞后于代码"的风险在本项目中不止一处。

---

## 2. 架构与目录结构

### 2.1 整体结构（健康）

```
01_源码/
├─ frontend_jingjingshangri_app/   Flutter，lib/ 分 pages(40) / widgets(20) / services / utils / theme
├─ backend_server/                 Express 4，routes(25) / services / middleware / migrations / jobs
└─ deploy_ops/                     267 个部署运维脚本（⚠️ 见 §6.1）
02_项目档案/                        31 份治理文档
03_关键报告与设计/                   13 份
```

前后端职责边界清晰，路由/中间件/服务/迁移分层明确，无跨层纠缠。**这部分是按规范做的。**

### 2.2 结构性缺口（三个空壳）

| 路径 | 实测 | 影响 |
|---|---|---|
| `lib/models/` | **空目录，0 文件** | 全项目无数据模型层，领域对象靠 `Map<String,dynamic>` 传递 |
| `lib/routes/` | **空目录，0 文件** | 路由表内联在 `app.dart`，无集中路由常量 |
| `lib/pages/invest/` | **空目录，0 文件** | 死目录残留（且项目标榜"永久去投资化"，此命名本身易生歧义） |

**无 models 层的连锁反应**（`app.dart` 全部 6 处页面入参）：

```dart
// lib/app.dart:63,65,75,76,77
case '/endorsement': return _route(EndorsementPage(human: args is Map<String, dynamic> ? args : const {}));
case '/chat':        return _route(ChatPage(human: args is Map<String, dynamic> ? args : const {}));
case '/human-detail':return _route(HumanDetailPage(human: args is Map<String, dynamic> ? args : const {}));
```

后果：**编译期零保护**。字段改名/类型变更不会报错，只会在运行时拿到 `null` 或错误类型；`const {}` 兜底会把"传参错误"静默伪装成"空数据页"。40 个页面规模下，这是后续维护的主要成本来源。

**路由双轨**：`app.dart` 只有 28 条命名路由，缺 `/login`、`/home`、`/profile`、`/messages`、`/projects`、`/checkout` —— 这些页面走 `Navigator.push(MaterialPageRoute)` 直接推。结果是**无法统一做路由守卫、deep link 与埋点**。

---

## 3. 核心模块评价

### 3.1 支付模块 —— 做得好（值得肯定）

`routes/pay.js` 的支付宝回调链路是**本次审查中质量最高的部分**，五道关口齐全：

```javascript
// routes/pay.js:245-270
if (!alipay.verifyNotify(body)) { ... return; }        // ① RSA2 验签
if (!alipay.notifyAppIdMatch(body)) { ... return; }    // ② app_id 归属校验
if (!alipay.isSuccessTradeStatus(body.trade_status)) { ... }  // ③ 交易状态
const pay = db.prepare('SELECT * FROM payments WHERE pay_no=?').get(body.out_trade_no);
if (!yuanEqual(body.total_amount, fenToYuan(pay.amount_fen))) { ... }  // ④ 金额对账 fail-closed
// ⑤ 幂等：applyPaid() 内对 rawBody 做 sha256，相同回调直接返回
```

第 ④ 点的注释写明"NaN/缺失一律不等（fail-closed）"——**这是懂行的人写的**。支付回调最常见的低级错误（金额缺失时被当成 0 而误判通过）被显式规避。

### 3.2 金额处理 —— 全库整数分，无浮点金额

```sql
-- migrations/mysql/01_schema_mysql8.sql
39:  service_fee BIGINT DEFAULT 0,      -- 分
42:  price BIGINT DEFAULT 9900,         -- 分（99元起，艺人自定价）
76:  required_amount BIGINT NOT NULL,   -- 当前应缴保证金（分）
```

前端 `lib/utils/pricing.dart` 是合格的价格 SSOT，注释明确"页面禁止再手写 ¥99/¥4901 等字面量"，并提供 `intentDepositOf(order)` 做"接口优先、常量兜底"。**金额铁律的落地是真实的，不是文档宣称。**

⚠️ 但费率字段用 `DOUBLE`（`platform_share 0.10`、`service_rate 0.05`、`tax_rate`），参与分账计算时存在精度风险。量级小，属【轻微】。

### 3.3 AI 成本防护 —— 设计正确

`routes/ai.js:29-37` 用了文件级中间件组合，而不是逐路由手写：

```javascript
function costGuard(req, res, next) {
  const allowAnon = process.env.ALLOW_ANON_AI === 'true';
  if (!req.userId && !allowAnon) return res.status(401).json({ code: 401, message: '生成接口需登录后使用' });
  next();
}
router.use(optionalAuth);   // 注释明确：必须先解析登录态，再做成本保护
router.use(costGuard);
```

注释里还记录了踩过的坑（V12.3 因中间件顺序颠倒导致"AI 层整体不可用"）。**AI 接口直接烧钱，这里做了正确的默认拒绝。**

---

## 4. 安全问题（按严重度）

### 4.1 【P0】生产服务器 root 密码明文留在包内，且脱敏声明失实

```python
# 01_源码/deploy_ops/_deploy_upload.py:3
HOST = "<生产IP>"; USER = "root"; PWD = "<明文口令，已脱敏>"
```

同一凭据出现在 **4 个文件**：
- `_deploy_upload.py`、`_ssh.py`、`_sftp.py`、`_r.py`

而 `包内清单_README.txt:16` 白纸黑字写着：

> 敏感项扫描结果：全部通过，未发现 .env / secrets目录 / conn.json / **明文密码文件泄漏**

**这比泄露本身更严重**：它证明交接方的脱敏扫描流程存在漏检，因此**包内"已剔除敏感项"的声明整体不可信**，承接方必须自行全量重扫，不能依赖该声明。

**影响**：任何拿到这个包的人（含传输链路上的任何人）都能直接 root 登录生产服务器，可窃取生产数据库、篡改支付回调、替换 APK。

**处置**：立即改 root 密码（或改用 SSH key 并禁用密码登录）→ 轮换 JWT_SECRET / FIELD_ENC_KEY → 检查 `/opt/jjsr` 下 `.env` 是否已被非授权访问 → 全量重扫交接包。

### 4.2 【P1】实名认证存在"未核验也判通过"的分支

```javascript
// routes/identity.js:58-62
} else {
  // 当前 provider 未就绪（火山 rms 审批中或缺凭证/阿里云缺 AK·场景ID）：维持历史行为自动通过，但显式标注"未真核验"
  status = 'approved';
  verifiedBy = 'unverified_auto';
}
```

注释说明这是**灰度期的有意设计**（保证流程可走通），且留了 `unverified_auto` 标记可追溯。工程意图没问题，但风险是：**只要 `idVerify.ready()` 为 false（缺 AK / 审批未过 / 上游故障），任何用户的实名状态都会被置为 approved。**

若下游有"已实名才能提现/接单/发布"的判断，**缺凭证期间这些权限等于全开**。建议：把 `unverified_auto` 状态在权限判定处显式排除，而不是只在数据库里留个标记。

### 4.3 【P1】敏感字段加密缺 fail-fast，配置遗漏不报警

```javascript
// utils/crypto.js:5
const KEY = Buffer.from(process.env.FIELD_ENC_KEY || 'dev_field_enc_key_32bytes_pad!!', 'utf8').slice(0, 32);
```

算法选型是对的（AES-256-GCM，随机 12 字节 IV，带 authTag）。**问题在回退值**：生产若漏配 `FIELD_ENC_KEY`，身份证号会用一段写死在源码里的公开常量加密，且**服务照常启动、不报错、无任何告警**。

对比 JWT 的处理（`config.js:13-15` 生产环境缺 `JWT_SECRET` 直接抛异常），这里明显不一致。**建议照抄 JWT 的 fail-fast 写法。**

### 4.4 【P1】后台管理员登录无限流

```javascript
// routes/admin.js:9
router.post('/login', (req, res) => {   // ← 无 limits.*
```

对比 `routes/auth.js:14` 的用户端登录 `router.post('/wechat', limits.login, ...)` 是**有限流**的。
后台登录口反而裸奔，可直接暴力破解管理员密码。全局限流（`app.js:51`）粒度太粗，撑不住针对性爆破。

### 4.5 【P2】旧支付链路遗留越权（文档已自认未修）

```javascript
// routes/payment.js:64
router.get('/status/:txNo', auth, (req, res) => {
  const txn = db.prepare('SELECT ... FROM payment_transactions WHERE tx_no = ?').get(req.params.txNo);
```

有 `auth`，但**只按 tx_no 查、不带 user_id 过滤** → 任何登录用户可遍历他人交易流水。
交接总览 §9.2 第 3 条已记录此项（"补归属过滤（低危）"），**属已知未修**。

### 4.6 【P2】其他

- `middleware/auth.js:11` `jwt.verify` 未指定 `algorithms: ['HS256']`（未锁算法白名单）
- `app.js:92` 错误响应直接回传 `err.message`，可能泄露 SQL 等内部细节
- `app.js:98` `app.listen(config.port, '0.0.0.0')` 写死，与文档"Node 绑定回环"的说法不符
- `db.js` 外键需逐连接开启；MySQL DDL 中 **0 个 `FOREIGN KEY` 约束**（61 张表全无外键）

---

## 5. 前端性能隐患（这是项目自称的主战场）

已实测的 6 条，均为真机卡顿的典型来源：

| # | 问题 | 位置 | 证据 |
|---|---|---|---|
| 1 | **列表单元件级实时毛玻璃** | `widgets/glass_card.dart:177` | 每张 `GlassCard` 内含 `BackdropFilter(ImageFilter.blur)`，滚动时逐帧重采样背景 |
| 2 | 主 Tab 关闭后仍跑动画 | `main_scaffold.dart:88-98` | `IndexedStack` 的**兄弟节点**（SliverAppBar 背景）仍在 layout/paint |
| 3 | **控制器泄漏** | `widgets/region_picker.dart:167` | `ListView.builder(controller: ScrollController(), ...)` —— 每次 build 新建、从不 dispose，且此处根本用不到 controller |
| 4 | **控制器泄漏** | `pages/profile/settings_page.dart:243` | `_showServerUrlDialog()` 内 `TextEditingController` 未释放，每次打开泄漏一个 |
| 5 | 低端机无降级 | 全局 | 无 BackdropFilter 分级策略，低配机与旗舰机同一套渲染路径 |
| 6 | **缓存零件已造未装** | `services/resource_cache.dart` | 全局检索：仅被自身单测引用，**零业务页面接入**（文档说法属实） |

关键代码片段：

```dart
// widgets/glass_card.dart:176-179  ← 每个卡片一个毛玻璃
child: BackdropFilter(
  filter: ImageFilter.blur(sigmaX: sigma, sigmaY: sigma),
```

```dart
// widgets/region_picker.dart:167  ← 每次 build 泄漏一个 ScrollController
return ListView.builder(
  controller: ScrollController(),
```

**关于"等真机 profile 才动手"**：项目方坚持先测量后修改，这个原则是对的。但上述 1/3/4 三条属于**不需要 profile 也能确定的缺陷**（泄漏就是泄漏，毛玻璃在低端机就是慢），可以不等测量直接修。

顺带：前端 `lib/` 共 5 处 `BackdropFilter`、16 个 `AnimationController`、182 处 `setState`、131 处 `mounted` 检查——**`mounted` 覆盖密度不错，说明异步安全意识是在线的**。

---

## 6. 依赖配置与工程卫生

### 6.1 项目卫生：200+ 临时产物未清理

| 位置 | 实测 |
|---|---|
| `frontend_jingjingshangri_app/` 根目录 | **100+ 个** `_an*.txt` / `_ana*.txt` / `_shots*.txt` / `_test_*.txt` / `_patch_*.cjs` / `_fix_*.cjs` / `_final*.bat` |
| `deploy_ops/` | **267 个文件**，其中 `log_r41~r85.cjs` 共 45+ 个（逐轮日志写入脚本） |
| `backend_server/_backup_v125_20260831043229/` | 历史备份目录留在包内 |

这些是开发过程的"脚手架残骸"。前端 `.gitignore` 其实**已经**忽略了 `_*.cjs`，但因为剥离 `.git` 后文件实体仍在包里——**说明打包是按目录拷贝，而不是按版本库导出**，这本身就是个流程问题。

对承接方的实际影响：新成员打开工程根目录看到 100 多个 `_ana_r7.txt`，无法判断哪些是活的；`_patch_v125a~k.cjs` 这类一次性 codemod 脚本混在根目录，有误执行风险。

### 6.2 后端依赖问题

```json
// backend_server/package.json
"dependencies": { "helmet": "^7.2.0", "localtunnel": "^2.0.2", "multer": "^1.4.5-lts.1", ... }
```

| 问题 | 说明 |
|---|---|
| **`helmet` 是死依赖** | 全仓 0 处引用。安全头在 `app.js:13-22` 手写实现（手写了 nosniff/frame-options/referrer-policy，但缺 `X-XSS-Protection` 等；CSP 对纯 API 服务也无实际意义） |
| **`localtunnel` 进生产依赖** | 内网穿透工具，被 `lt_start.js` / `tunnel.js` 使用。开发期把本地服务暴露公网，属高风险工具，不应在生产依赖树 |
| **无 `devDependencies`** | 所有依赖混装，无 eslint / prettier / jest 等开发依赖 |
| **`multer` 1.x** | 1.x 已 EOL，历史 CVE 较多，且承担文件上传 |
| 无 `engines` 字段 | 未约束 Node 版本 |

### 6.3 前端依赖：一个需要记入技术债的"钉子"

```yaml
# frontend_jingjingshangri_app/pubspec.yaml:17,39-41
video_player: 2.9.2  # 精确固定
dependency_overrides:
  video_player_android: 2.12.0   # 钉版本，规避 Kotlin 2.4(K2) 下 pigeon 重复声明
```

用 `dependency_overrides` 钉死传递依赖，是典型的"编译不过就钉住"应急做法。**副作用：整个依赖树都无法升级 video_player_android**，后续升级 Flutter/AGP/Kotlin 时会再次撞墙，且 `dependency_overrides` 会掩盖真实冲突直到爆发。应记录为明确的技改项。

---

## 7. 测试与质量保障（最大短板）

| | 前端 | 后端 |
|---|---:|---:|
| 测试文件 | 62 | **5** |
| 代码行数 | 5,976 | **438** |

后端 5 个测试文件的覆盖范围：

```
test/finance.test.cjs       134 行   ← 资金
test/migrations.test.cjs    140 行   ← 迁移
test/ai_deliverable.test.cjs 100 行  ← AI 交付物
test/providers.test.cjs      34 行
test/storage.test.cjs        30 行
```

**未覆盖的关键路径**：鉴权（`middleware/auth.js`）、支付回调（`routes/pay.js` 五道关口）、订单状态机、实名认证（`routes/identity.js`）、权限校验（越权）。

对一个**涉及真实资金、实名信息、人脸材料**的系统，438 行测试意味着：**任何重构都是在无网状态下走钢丝**。§3.1 里那个写得相当漂亮的支付回调，恰恰没有任何回归测试保护它。

前端 62 文件 / 5,976 行测试则相对扎实，`integration_test/golden_journeys_test.dart` 覆盖 3 条黄金链路。

---

## 8. 优点（真实成立，非客套）

1. **知识传承做得极好**。31 份项目档案 + r41–r85 共 45 轮逐轮迭代日志，"为什么这么改"基本都有据可查。多数交接项目做不到这一点——这是本项目**最值钱的资产**。
2. **支付回调安全五道关口齐全**（验签 → app_id → 状态 → 金额 fail-closed → 幂等），注释甚至写明"NaN/缺失一律不等"，是懂行的人写的。
3. **金额全库整数分**，无浮点金额字段；前端价格 SSOT 真实落地，不是文档口号。
4. **AI 成本防护默认拒绝**，且用文件级中间件统一兜底而非逐路由手写。
5. **合规设计有实质投入**：人脸单独授权不存原图、未成年人模式、内容机审、审计日志、永久去投资化措辞，均有对应表结构与代码，不是纸面合规。
6. **代码规模与文档声称高度吻合**（22,062 行 / 61 表 / 25 路由 / 40 页面全部实测一致），说明交接方的盘点是认真的。
7. **`mounted` 检查覆盖 131 处**，异步安全意识在线。
8. **铁律意识强**：不引入 Riverpod/BLoC、先测量后修改、mock 与真实数据分离——这些约束避免了项目在末期失控。

---

## 9. 改进路线（按投入产出排序）

### 第 0 周：止血（安全，不可延后）
1. **轮换服务器 root 密码 / 改 SSH key 登录**；轮换 `JWT_SECRET`、`FIELD_ENC_KEY`；排查 `/opt/jjsr/.env` 是否已被访问
2. 全量重扫交接包（不能信原扫描结论），清理 4 个含密码文件
3. 确认 `unverified_auto` 分支在权限判定处是否已被排除

### 第 1–2 周：低成本高收益
4. `utils/crypto.js` 补 fail-fast（照抄 `config.js:13-15` 的 JWT 写法）
5. `routes/admin.js:9` 补 `limits.login`
6. `payment.js:64` 补 `AND user_id=?`（文档已列的已知项，顺手修）
7. 修 3 处前端泄漏：`region_picker.dart:167`（删掉这个无用的 ScrollController）、`settings_page.dart:243`、`glass_card.dart` 列表场景降级
8. 清理 100+ 临时文件与 267 个运维脚本中的一次性产物，保留 `deploy_release.sh` / `https/` / `ops/` 常备部分

### 第 3–6 周：结构性补强
9. **补后端测试至覆盖四条命脉**：鉴权、支付回调、订单状态机、越权校验。目标不是覆盖率数字，而是让重构有网
10. **建 `lib/models/` 数据模型层**，改造 `app.dart` 6 处 `args is Map<String,dynamic>` 强转（可增量：新页面用模型，老页面逐步迁移）
11. **接入 `ResourceCache`**（零件与单测已就绪，无需等真机 profile）
12. 清理依赖：`helmet` 移除或真正启用、`localtunnel` 移入 devDependencies、补 `engines`、评估 `multer` 升级到 2.x

### 长期
13. `dependency_overrides` 的 video_player 钉子，随下次 Flutter/AGP 升级一并解决
14. MySQL DDL 补外键约束（当前 61 表 0 外键），或明确文档化"由应用层保证引用完整性"
15. CI/CD：当前是脚本化发布，需补自动化流水线与密钥扫描门禁
16. APM / 崩溃监控 / 埋点看板（当前完全缺失，真机卡顿只能靠用户反馈）

---

## 10. 承接方 Checklist

- [ ] **已改 root 密码并轮换全部密钥**（P0，先做这个再谈别的）
- [ ] 已自行全量重扫凭据，未依赖原"扫描通过"结论
- [ ] 已确认：不要照 §9.1 去重改 `main_scaffold.dart` 的 IndexedStack（代码已修，文档滞后）
- [ ] 已确认：`lib/models/`、`lib/routes/`、`lib/pages/invest/` 三个空目录是缺口而非遗漏拷贝
- [ ] 已确认：后端测试只有 438 行，重构前需自行补网
- [ ] 已确认：服务器 2026-09-27 到期（距今 18 天）
- [ ] 已确认：keystore 密码、第三方凭证需线下向甲方索取
- [ ] 已确认：支付仍为沙箱、阿里云/火山凭证缺失，"可运营"部分约 20%

---

*本报告基于静态审查，未执行构建与测试。所有定量数据（行数/表数/文件数）均为实测；所有定性判断均附代码位置可复核。*
